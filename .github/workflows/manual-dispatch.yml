name: Manual CI Dispatch (prism-spectra)

on:
  workflow_dispatch: {}

jobs:
  run-tests:
    uses: devknowsdev/workspace-meta/.github/workflows/reusable-run-tests.yml@main
    with:
      node_version: '22'
      test_commands: 'npm ci && npm test'
